var _ = require('lodash');

var Item = function( stub ){
  this.name = '';
  this.desc = '';
  this.effects = [];
  this.type = '';

  if ( typeof stub === 'object'){
    _.merge( this, stub );
  }
};